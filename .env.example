# Resend API Key
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=

# Default sender email (optional - can also be provided in request)
# Must be a verified domain in your Resend account
FROM_EMAIL=

# Optional: Default sender name
FROM_NAME=internationalsoftwareleasing
